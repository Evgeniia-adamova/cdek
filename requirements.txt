# Core for sentiment (text_analysis.py: ruBERT)
torch>=2.0.0
transformers>=4.30.0

# Speaker diarization via pyannote (requires HUGGINGFACE_TOKEN in .env)
pyannote.audio>=3.1.0
soundfile>=0.12.0
# Bundled FFmpeg binaries for audio decoding (OGG Opus) without system FFmpeg
imageio-ffmpeg>=0.4.0

# SpeechKit async API + S3 upload
requests>=2.28.0
boto3>=1.26.0

# PostgreSQL (Yandex Managed)
psycopg2-binary>=2.9.0

# Celery task queue + Redis broker/backend
celery[redis]>=5.3.0
redis>=5.0.0
